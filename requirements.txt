# EPR Imaging App — Python dependencies
# Install with: pip install -r requirements.txt
# Recommended: Python 3.11 or 3.12

# GUI framework
PySide6>=6.7

# Data handling
numpy>=1.26
pandas>=2.2

# Video processing (frame extraction)
opencv-python>=4.10

# Signal interpolation and scientific computing
scipy>=1.13

# Ordinary Kriging interpolation for 2D and 3D sensor outputs
pykrige>=1.7

# Geospatial raster output (GeoTIFF generation)
rasterio>=1.3

# UTM coordinate projection (lat/lon → easting/northing)
utm>=0.7

# 3D point cloud and PLY file output (used by PointCloudPipeline)
plyfile>=0.9

# PNG image writing for raster slices (thread-safe; avoids pyplot Qt conflict)
Pillow>=10.0

# Colour mapping for point cloud / raster visualisation
matplotlib>=3.8

# Timeline plot widget
pyqtgraph>=0.13

# Anthropic API client (Claude AI assistant panel)
anthropic>=0.25

# 3D interactive viewer: point cloud / mesh overlay (PyVista + Qt interactor)
pyvista>=0.44
pyvistaqt>=0.11
